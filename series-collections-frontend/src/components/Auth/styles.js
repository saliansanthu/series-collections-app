import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    marginBottom: 10,
  },
  paper: {
    padding: 20,
    textAlign: "center",
  },
  input: {
    marginBottom: 10,
    marginTop: 5,
  },
}));
